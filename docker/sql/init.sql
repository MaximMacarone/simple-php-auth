CREATE TABLE users (
                       username VARCHAR(255) NOT NULL,
                       password VARCHAR(255) NOT NULL
);

CREATE TABLE hashed_users (
                       username VARCHAR(255) NOT NULL,
                       password VARCHAR(255) NOT NULL
);